# Flash Mall Superpowers 项目使用指南

本文件适用于整个 Flash Mall 仓库。使用 Superpowers 或其他代理工作流时，除非更高优先级指令明确要求，否则必须遵守以下项目约定。

## 1. 设计阶段不使用浏览器视觉伴侣

- 不询问用户是否需要浏览器展示、视觉伴侣、网页草图或并排方案预览；本项目对此类邀请的固定答案是“不使用”。
- 设计讨论直接使用文字、必要的 Markdown 表格或 Mermaid 图完成。
- 这条规则只禁止在设计讨论中邀请或使用浏览器视觉伴侣，不禁止在实现完成后使用 Windows Chrome、Playwright 等工具进行真实页面验收。

## 2. 设计方案同时承担执行计划职责

- 不在设计方案之后再编写一份内容重复的执行计划，也不为“开始写执行计划”单独停下来请求确认。
- 设计方案获得用户认可后，直接进入实现并持续推进；只有缺少必要权限、存在不可逆操作或用户选择会实质改变结果时才暂停。
- 因为不再另写执行计划，设计方案必须足够具体、贴合当前代码，至少包含：
  - 当前实现与问题根因，引用真实文件、接口、表和运行拓扑；
  - 明确的目标、非目标、业务不变量和验收标准；
  - 需要修改、新增或删除的文件，以及每个文件承担的职责；
  - API、数据结构、数据库迁移、权限和错误处理规则；
  - 前后端数据流、服务边界及与 Hertz、Go-zero、Kitex 的关系；
  - 实施顺序、兼容策略、数据恢复或回滚方式；
  - 构建、部署、真实运行观察和最终验证命令；
  - 提交边界，以及是否推送、创建 PR 或合并分支。
- 执行时可在内部维护进度清单，但不要把它变成第二份计划文档或第二轮审批门槛。

## 3. 一次性完整具体化修改意见

- 当用户的修改意见需要具体化时，一次性列出全部需要确认的事项，不要每次只问一个问题。
- 同一轮内容应包含：已确认需求、待确认选择、合理默认值、各选择的影响，以及推荐方案。
- 能从当前代码、数据库、配置、运行环境或既有决定中确定的内容，先自行核实，不向用户重复提问。
- 对安全且可逆的细节采用推荐默认值继续推进；只有会改变产品方向、造成不可逆影响或扩大授权范围的事项才等待用户答复。
- 用户一次回复后，应合并其全部意见更新方案并继续执行，避免反复进行碎片化确认。

## 推荐工作流

1. 读取当前代码、Git 状态、运行拓扑和唯一有效的项目现状文档。
2. 一次性整理需求、假设、选择与推荐方案。
3. 输出可直接执行的详细设计方案并获得一次认可。
4. 直接实施方案，持续处理实现中发现的可逆问题。
5. 完成构建、真实链路、浏览器验收和必要测试。
6. 恢复临时验收数据，更新当前项目文档，提交代码后统一汇报。
